const a = 3; // integer
const b = 2.5; // float
// Operations
const sum  = a + b;
const ex   = a - b;
const mult = a * b;
const div  = a / b;
const pow  = a ** b;

const minusA = -a; // => -3

// *** JS Magic *** //
const magic = 0.1 + 0.2 !== 0.3;

const priority = (a - b) * (a + b);
// NaN
console.log(typeof NaN);
